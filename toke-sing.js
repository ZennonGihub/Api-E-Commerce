/* const jwt = require('jsonwebtoken');

const secret = 'soto';
const payload = {
  sub: 1, //Identificador de usuario
  role: 'customer',
};

function signToken(payload, secret) {
  return jwt.sign(payload, secret);
}

const token = signToken(payload, secret);
console.log(token);
*/
